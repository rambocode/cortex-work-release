# 官网截图清单

## 当前状态（2026-08-11）

11 张已到位并接进页面：01、02、04、05、06、07、08、09、11、12、13。
原图在 `~/Desktop/cortex-work/`，压成 webp 后落在 `public/shots/`，登记在 `lib/shots.ts`。

两处**没有用截图**，改成了 CSS 自绘的深色示意图（都带「（示意）」字样，不冒充真图）：

| 位置 | 组件 | 为什么不用截图 |
|---|---|---|
| 03 会话过程时间线 | `components/mocks/TimelineMock.tsx` | 拍到的那张是浅色主题、内容整片打码，读不出任何信息 |
| 10 电脑操作确认框 | `components/mocks/ConfirmMock.tsx` | 没拍到。这块是全站最重要的信任证据，自绘能把动作/目标/坐标三行摆清楚 |

想换回真截图：把图放进 `public/shots/`，在 `lib/shots.ts` 加一条，再把对应区块里的
`<TimelineMock d={d} />` / `media={<ConfirmMock d={d} />}` 换成 `<Shot code="…" …/>` 即可。

**⚠️ 已知问题：现有截图全是浅色主题**，而官网恒为深海深色。已经把截图外框调成中性亮描边
来缓解，但白底大图在深色页面上仍然偏刺眼。要彻底解决，需要把 App 切到
设置 › 通用 › 主题 = 深色 后重拍。

---

## 重拍/补拍时的规则

拍好后按**文件名**放进 `site/public/shots/`（或丢给我，我来压缩、登记、接线）。

## 通用要求

| 项 | 要求 |
|---|---|
| 设备 | Retina 屏（2x）。macOS 截窗口用 `⌘⇧4` 然后按空格点窗口，会自动带阴影和圆角 |
| 主题 | **App 用深色主题**（设置 › 通用 › 主题 = 深色）。官网恒为深海深色，浅色截图放上去会刺眼 |
| 窗口栏 | 截整窗（自带红黄绿灯）或截局部都行。站点的截图框默认**不再叠加**假的窗口栏，不会出现两层红黄绿灯 |
| 格式 | PNG 即可，不用自己压——我会用 `magick` 转成 webp 并按展示尺寸缩放（现在 11 张合计不到 800KB）|
| 窗口宽度 | 主窗口类截图请把窗口拉到 **1440×900 左右**，太窄侧栏会自动收起，太宽三栏会显得空 |
| 阴影 | 不用自己加边框/圆角/阴影，官网的截图框会包一层描边和光晕 |
| **脱敏** | 真实项目名、文件路径、邮箱、API Key、公司名、聊天内容里的人名——都换成示例内容。API Key 一律只留 `sk-••••••••` |
| 内容真实性 | 尽量用真的跑一遍的结果，不要摆空态。空荡荡的界面没有说服力 |

缺哪张就把哪个占位框留着（或退回 CSS 示意图），不影响其余部分上线。

---

## SHOT-01 · 主窗口全景（Hero 主图）

- **文件名**：`01-workspace.png`
- **建议画布**：1440×900
- **拍什么**：主窗口三栏全景。左栏项目/会话列表、中栏一段**有内容的会话**（最好带一次工具调用或一张产物卡）、右栏 Rail 可见。
- **注意**：这是首屏第一眼，值得多摆几次。会话内容挑一个能一眼看懂在干活的（比如「帮我把这季度数据做成 8 页汇报 PPT」）。

## SHOT-02 · 语音球（Voice 区块）

- **文件名**：`02-voice-orb.png`
- **建议画布**：1000×700，**连桌面背景一起截**（要让人看出它是浮在桌面上的）
- **拍什么**：语音球处于 `speaking` 或 `listening` 状态，球体上方的磨砂浮层里有一段正在滚动的回复文字。
- **注意**：桌面背景挑深色纯色或深色壁纸，别是花花绿绿的照片。桌面上的其他图标最好清一下。

## SHOT-03 · 会话过程时间线（Workspace 区块）· 目前是 CSS 自绘示意图

- **文件名**：`03-timeline.png`
- **建议画布**：1000×760（**只截中栏那一列**，不用整窗）
- **拍什么**：一轮回复的过程时间线——思考、工具调用胶囊、终端块、子代理委派卡片，最好几种都出现。有阶段折叠更好。
- **注意**：上一版拍的是浅色主题且内容整片打码，用不了。重拍要求：**深色主题**，工具名与参数至少有几行是可读的（真不能露的换成示例项目再跑一次，别打码）。

## SHOT-04 · 会话四向分屏 `[可选]`

- **文件名**：`04-split.png`
- **建议画布**：1440×900
- **拍什么**：3–4 个会话窗格同屏（比如左右两栏 + 右侧上下分），每个窗格里都有不同内容。
- **注意**：拍摄成本高（要摆好几个会话），拍不了就跳过。

## SHOT-05 · 演示文稿（Deliverables tab 1）

- **文件名**：`05-slides.png`
- **建议画布**：1440×900
- **拍什么**：生成好的演示文稿在画布/编辑器里，左侧页面缩略图列、右侧当前页。挑一套**好看的深色模板**。
- **注意**：这张最能打，值得挑一份内容质量高的稿子。

## SHOT-06 · 深度研究报告（Deliverables tab 2）

- **文件名**：`06-research.png`
- **建议画布**：1200×860
- **拍什么**：右栏研究报告面板，正文里能看到**引用角标**，最好同时展开一个引用的原文预览。
- **注意**：研究主题挑个通用的（如「2026 年国内 AI 编程助手市场格局」），别用公司内部课题。

## SHOT-07 · 写作台改稿 diff（Deliverables tab 3）

- **文件名**：`07-writing.png`
- **建议画布**：1440×900
- **拍什么**：写作台三栏，正文里有**行级红绿 diff**、旁边是逐块接受/拒绝的按钮。
- **注意**：如果能同时露出灰字续写或 ⌘K 改写框更好，但别为此把画面塞太满。

## SHOT-08 · 设计稿生成（Deliverables tab 4）

- **文件名**：`08-design.png`
- **建议画布**：1440×900
- **拍什么**：设计稿编辑器 / 画布，一张生成出来的网页或组件设计稿，实时预览可见。
- **注意**：挑视觉完成度高的一稿。

## SHOT-09 · 子代理委派（Squad 区块）

- **文件名**：`09-subagents.png`
- **建议画布**：1200×860
- **拍什么**：二选一或拼一张 ——
  - 会话里**多个子代理卡片并行跑**、各自显示进展；或
  - 子代理管理面板（角色目录 + 头像 + 职责）
- **注意**：能同时看到「派出去了好几个」最好，这是这块的论点。

## SHOT-10 · 电脑操作确认弹窗（Hands 区块）· 目前是 CSS 自绘示意图

- **文件名**：`10-computer-use.png`
- **建议画布**：1200×860
- **拍什么**：Computer Use 要点击/输入前弹出的**逐次确认框**，最好背景能看到它正要操作的那个 app。
- **注意**：这是全站最重要的信任证据——「AI 能动你的电脑，但每一步都要你点允许」。确认框里的动作描述要清晰可读。

## SHOT-11 · Loop 工作流画布（Automation 区块）

- **文件名**：`11-workflow.png`
- **建议画布**：1440×900
- **拍什么**：一条**接得比较完整**的工作流：触发节点 → AI Agent → 条件分支 → HTTP/输出，节点之间连线清楚。有人工审批节点更好。
- **注意**：别用只有两个节点的 demo，要能看出「这玩意能编排真东西」。

## SHOT-12 · 快捷回复气泡 `[可选]`

- **文件名**：`12-quick-reply.png`
- **建议画布**：1200×800，**连第三方 app 一起截**
- **拍什么**：在微信/邮件窗口上方浮着快捷回复的候选草稿气泡。
- **注意**：微信聊天内容必须是自己和自己/测试账号的对话，头像和昵称要脱敏。拍不了就跳过。

## SHOT-13 · 闪记浮层（Capture 区块）

- **文件名**：`13-notes.png`（原图叫 `13-shanji.png`）
- **已到位**。浮在桌面壁纸上的闪记输入框，底部带项目选择器。

---

## 脱敏复查（现有几张里还留着的）

这几处是真实内容，上线前值得确认一下能不能公开：

- `12-quick-reply`：微信窗口里是一段真实对话（「自动结束了，您看后面要继续吗」等）。
- `13-notes` / `01-workspace`：露出了真实项目名 `knowledge-base-system`、`cheng-xi-meditation`、`keep-vibe`、`Tandem`。
- `05-slides`：地址栏里有完整本地路径，含用户名 `mike`。

要换的话重拍对应几张即可，其余不受影响。

## 拍完之后

把文件丢进 `site/public/shots/` 就行，命名照上面的。缺哪张我就把哪个占位框留着，不影响其余部分上线。
